iSpeak Confidence V17.2.0 — Hardening & Marketplace Foundation

Protected baseline: V17.1.0.
AI/API and SMTP/email configuration were intentionally left for the owner to connect later.

Implemented in this pass:
- Fixed local-date streak/activity bug caused by UTC date slicing.
- Added V17.2 state schema/key with migration fallback from older local state.
- Added Library reading notes to cloud-synced state.
- Updated stale placement metadata to V17.2.0.
- Fixed advanced conversation to use the mascot for the currently selected learning language.
- Replaced Cambodia-only booking conversion with IANA timezone-aware conversion.
- Made server availability calculations use each teacher's stored IANA timezone.
- Expanded visible booking availability from 14 days to 60 days.
- Approved teacher applications now hydrate dynamically into the learner marketplace.
- Approved-teacher API now exposes stable application ID, country and timezone.
- Teacher qualification/certificate files are private; applicant/admin authorization is required.
- Added basic file-signature validation for applicant uploads instead of trusting MIME labels alone.
- Replacing teacher photo/video removes the previous stored upload.
- Signed-in student bookings are linked to account email and can refresh live server status.
- Applications Admin control is hidden from the normal learner UI; use ?admin=1 locally when needed.
- Added visible keyboard focus treatment and reduced-motion support.
- Added accessible label to booking dialog close button.
- Study-time statistics now use elapsed active block time (bounded 1–60 minutes) rather than a fixed 10 minutes.
- Offline cache version bumped to V17.2.0.
- Added automated V17.2 regression checks and made the BAT run them before starting.

Important remaining major work:
- Move JSON persistence to a proper production database.
- Make certificates fully server-authoritative with verification URLs.
- Expand real curriculum depth substantially (especially non-Arabic courses).
- Rewrite/de-template the IELTS content bank.
- Build real language-specific grammar maps and richer authored Stories.
- Replace browser TTS for premium listening content with controlled native audio.
- Build stronger pronunciation analysis once the chosen AI/speech stack is connected.
- Complete unified i18n/localization and RTL QA.
- Add full browser/device end-to-end and visual regression tests.
